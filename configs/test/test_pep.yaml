dataset:
  test:
    class: PeptideDataset
    mmap_dir: ./datasets/peptide/LNR/processed

dataloader:
  num_workers: 4
  batch_size: 8

n_samples: 100
n_cycles: 0

# Generation checkpoint selector. Passing --ckpt overrides checkpoint_dir.
axial_type: cross
axial_position: Both
checkpoint_dir: ./checkpoints
